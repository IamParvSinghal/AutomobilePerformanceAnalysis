export function Hero() {
  return (
    <section className="hero-panel">
      <h1>Estimate a car&apos;s fuel efficiency from its specs.</h1>
      <p className="hero-copy">
        Enter details like engine size, weight, year, and origin, and the app predicts how far the
        car can travel for the fuel it uses.
      </p>
    </section>
  );
}
